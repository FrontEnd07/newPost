import React from 'react';
import style from "./Dashboard.module.scss";

const Dashboard = () => {
    return <div className={`${style.main && style.main}`}>
        
    </div>
}

export default Dashboard;
